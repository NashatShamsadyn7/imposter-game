# Dependencies
node_modules

# Build output
dist
dist-ssr

# graphify knowledge-graph artifacts (regenerable)
graphify-out

# Generated word-bank files (regenerate with npm run bank / npm run images)
public/word-bank-local.json
public/word-bank-static.json

# Stitch design mockups (reference only, large binaries)
stitch.zip
stitch_extracted

# Supabase CLI local state
supabase/.temp
supabase/.branches

# Environment / secrets
.env
.env.*
!.env.example

# Claude Code local config (may contain tokens)
.claude/settings.json
.claude/settings.local.json

# Codex / MCP local config (contains live access tokens)
.codex

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Editor / OS
.DS_Store
.vscode/*
!.vscode/extensions.json
.idea
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# ───── ئەپی ئەندرۆید ─────
dist-offline/
android/keystore.properties
android/app/*.keystore
android/app/*.jks
android/local.properties
