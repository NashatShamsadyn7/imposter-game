<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!-- screenOrientation=portrait: مۆبایلەکە دەست بە دەست دەگوازرێتەوە،
             سووڕانەوەی شاشە تەنها تێکی دەدات. -->
        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:screenOrientation="portrait"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>
    </application>

    <!-- ═══════════════════════════════════════════════════════
         ئیزنەکان

         INTERNET: تەنها بۆ پشکنینی وەشان لە کاتی کردنەوەی ئەپ
         (یەک داواکاری بچووک بۆ imposter-version.json). یارییەکە
         خۆی — وشەکان، وێنەکان، فۆنتەکان — تەواو لەناو APK ـەکەدان
         و هەرگیز داواکاری تۆڕ ناکەن.

         ⚠️ گرنگ: ئەگەر پشکنینەکە شکستی هێنا یان ئینتەرنێت نەبوو،
            ئەپەکە بە ئاسایی دەکرێتەوە و یاری دەکرێت. هەرگیز
            یاریکردن ڕاناگیرێت بەهۆی نەبوونی تۆڕەوە — ئەوە
            هەموو مەبەستی ئەپەکە لەناو دەبات.
         ═══════════════════════════════════════════════════════ -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- لەرینەوەی هاپتیک لە کاتی کرتە و ئاشکراکردنی ڕۆڵ -->
    <uses-permission android:name="android.permission.VIBRATE" />
    <!-- شاشە بەخەبەر بمێنێتەوە لە کاتی یاری (Screen Wake Lock API) -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
</manifest>
