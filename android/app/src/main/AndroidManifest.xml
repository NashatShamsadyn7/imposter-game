<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!-- screenOrientation=portrait: مۆبایلەکە دەست بە دەست دەگوازرێتەوە،
             سووڕانەوەی شاشە تەنها تێکی دەدات. -->
        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:screenOrientation="portrait"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>
    </application>

    <!-- ═══════════════════════════════════════════════════════
         ئیزنەکان

         ⚠️ android.permission.INTERNET بە ئەنقەست لێرەدا نییە.
            ئەم ئەپە هەرگیز پەیوەندی بە تۆڕەوە ناکات — هەموو
            وشەکان، وێنەکان و فۆنتەکان لەناو APK ـەکەدان.
            WebView لە خۆماڵییەوە (WebViewAssetLoader) پێشکەش
            دەکرێت، کە داواکاری تۆڕ نییە.

            سوودی ئەمە: هەرکەسێک لە ڕێکخستنی ئەپدا دەبینێت کە
            ئەپەکە ئیزنی تۆڕی نییە — واتە بەڵێنی «بێ ئینتەرنێت»
            سەلماوە، نەک تەنها وتراوە.
         ═══════════════════════════════════════════════════════ -->

    <!-- لەرینەوەی هاپتیک لە کاتی کرتە و ئاشکراکردنی ڕۆڵ -->
    <uses-permission android:name="android.permission.VIBRATE" />
    <!-- شاشە بەخەبەر بمێنێتەوە لە کاتی یاری (Screen Wake Lock API) -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
</manifest>
